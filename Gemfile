# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in debugs_bunny.gemspec
gemspec

# Use Zetatango fork of attr_encrypted with Ruby 3.3 fixes
gem 'attr_encrypted', github: 'Zetatango/attr_encrypted', branch: 'tp-119140-upgrade-ruby-to-3.3.10'
